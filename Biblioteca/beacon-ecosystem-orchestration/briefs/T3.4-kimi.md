# T3.4 — Move nature samples out of digital-beacon into beacon-spatial assets

Agent: kimi. Size: S.
Repos: ~/Projects/digital-beacon (source) and ~/Projects/beacon-spatial (destination).

## Inventory (verified by orchestrator, 2026-07-18)

`digital-beacon/data/uploads/` contains:

- `dominicalito_frogs_pond.wav` (59.1 MB) — real nature sample used by SampleLayer.
- `06-30-2026 23.52.wav` (75.4 MB) — second uploaded WAV, content unreviewed; keep, do not delete.
- `dominicalito_frogs_pond_spectrogram.png`, `frogs_analysis_spectrogram.png`, `*_waveform.png` — regenerable analysis artifacts; do NOT move.
- `test.wav` (12 B), `test_tone.wav`, `test-upload.json` — test junk; do NOT move.

## Task

1. In beacon-spatial: create `assets/nature-samples/` (a directory that MUST NOT be tracked by git — add it to `.gitignore`).
2. Move (not copy) the two WAV files listed above into it.
3. Create `assets/nature-samples/MANIFEST.md` (tracked by git) listing: filename, byte size, SHA-256, and provenance note (`digital-beacon data/uploads`, moved T3.4, 2026-07-18). The manifest file is small and tracked; the WAVs are not.
4. Remove the two WAVs from digital-beacon (including from git history tracking if they are tracked — check `git -C ~/Projects/digital-beacon ls-files data/uploads/` first; if tracked, `git rm` them and leave the remaining PNG/test files untouched). If they are NOT tracked, just delete the files after the move and SHA-256 match verification.
5. Update beacon-spatial `README.md` Files table: add the assets directory with a line saying samples live outside git and how the manifest describes them. Update the `MODULATION_PRESETS.md` usage example path only if it references a sample path (check first).
6. Byte-verify: SHA-256 of moved files must match the source hashes recorded in the manifest.

## Constraints

- Two repos; two separate commits are expected (one per repo). Do NOT commit yourself — orchestrator audits and commits. Just leave both working trees in final state.
- No code changes anywhere.
- English only.

## Report

Exact file list moved with SHA-256 before/after, git tracked/untracked determination for the source files, and `git status --short` of both repos.
