# T2.4 — Shaper audio smoke, clipping characterization, and contract regression

Agent: codex (`gpt-5.6-sol`, xhigh). Size: M.
Repo: ~/Projects/harmonic-shaper.

## Authority (read first)

- `src/harmonic_shaper/synth_pure.py`: extracted NumPy reference renderer; its docstring records the historical rapid-transition normalization/clipping problem.
- `src/harmonic_shaper/audio_engine.py`, `state.py`, `voice_cache.py`: live engine and source of runtime behavior.
- Existing tests: `tests/test_synth_pure.py`, `tests/test_shaper_contract.py`, and the established contract codec/golden sidecar.
- Roadmap T2.4: suite green; clipping characterized; cheap fix only if evidence makes it clear, otherwise a documented issue.

## Task

1. Strengthen standalone render evidence using `synth_pure`:
   - deterministic one-voice and dense multi-voice prepared inputs;
   - outputs must preserve requested sample count, be finite, and remain in the declared output range;
   - explicitly measure peak, RMS, and any samples at/over full-scale, including a rapid active-voice-count transition scenario.

2. Exercise live Shaper normalization directly without audio hardware:
   - construct a `VoiceParameterStore` / `AudioEngine` callback-path test with rapid `voice_on`/`voice_off` and release-tail-like active histories;
   - verify no NaN/Inf and characterize the peak against the reference model.
   - Do not rely on sounddevice hardware; unit-test the callback/rendering logic as currently structured.

3. Diagnose discrepancies exactly. If a root cause has a small, well-tested local correction, implement it and add regression coverage. If not, add an English `docs/CLIPPING_DIAGNOSIS.md` including reproducible scenario, measured values, source location/hypothesis marked as such, and an explicit next decision. Do not hide a defect by arbitrary normalization.

4. Contract: keep `contracts/shaper.contract.json` and its golden sidecar verified. Add only relevant round-trip/golden tests; no incompatible `/digital/*` changes.

## Acceptance

- `pytest tests/ -q` passes.
- A reproducible, headless command/test emits numerical evidence for dense voices and transitions.
- No unsupported audible-audio claim.
- No commit.

## Report

Tests added, peak/RMS/clipping measurements for each scenario, whether a code fix was justified, and full test result.