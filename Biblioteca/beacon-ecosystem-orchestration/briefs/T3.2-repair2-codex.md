# T3.2 repair 2 — eliminate undeclared python-osc test dependency

Agent: codex (`gpt-5.6-sol`, xhigh). Repo: ~/Projects/beacon-spatial.

The first T3.2 repair correctly diagnosed OSC strings as SuperCollider `Symbol`, but its new test breaks the normal test environment:

```text
python3 -m pytest tests/test_osc_contract.py -q
ModuleNotFoundError: No module named 'pythonosc'
```

`test_nature.py` still runs; `test_osc_contract.py` cannot collect because it imports `from pythonosc.udp_client import SimpleUDPClient`. Do not merely skip the test or add an unrelated runtime dependency.

## Task

- Remove the undeclared `pythonosc` test dependency.
- Build and send the same standard OSC `s` string packet via an existing project codec or Python stdlib. It must remain a real UDP OSC wire message, not a direct sclang call.
- Keep the sclang probe assertion that the received valid OSC string is a `Symbol` and compatibility guard accepts it.
- Run from the repo root using bare `python3` (not a hidden venv):
  `python3 -m pytest tests -q`
- Then repeat the actual beacon/scsynth E2E sequence from the first repair brief if feasible, and report it.

## Constraints

- Do NOT commit.
- Do not weaken missing/non-string rejection or alter any unrelated behavior.
