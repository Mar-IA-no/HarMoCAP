# T0.1 — Fix committed merge conflicts in beacon-spatial/webui.py

Agent: codex (gpt-5.6-sol, xhigh). Size: M. Wave: 1.
Repo: ~/Projects/beacon-spatial (branch main, clean tree at dispatch).
Spec: ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/03-beacon-spatial.md §1 and §5.

## Task

`webui.py` (repo root, ~90 KB, Flask control UI on :5050) has UNRESOLVED MERGE CONFLICT MARKERS committed in HEAD (commit c8ce383): `<<<<<<< HEAD` / `=======` / `>>>>>>> 9fbe782` around lines 718–780 and 1725–1753. The file does not parse (`python3 -m ast` fails, SyntaxError reported at line 1753). The conflict is between:

- the `sensors` side (local unmerged branch `sensors`): the Sensor Interpreter logic (phone DeviceOrientation/DeviceMotion → control), and
- commit `9fbe782` (main side): a fix for the recording path (relative-path bug that produced the junk `beacon_record_260616_092811_/` directory).

**Resolve the conflict keeping BOTH functionalities: the sensors logic AND the recording-path fix.** You may inspect git history (`git log`, `git show 9fbe782`, `git show sensors:webui.py`, `git diff`) to reconstruct intent — that is in-scope. Do not touch any other file. Do not redesign anything else.

## Acceptance criteria

1. `python3 -m ast` parses `webui.py` cleanly (run: `python3 -c "import ast; ast.parse(open('webui.py').read())"`).
2. No conflict markers remain anywhere in the repo (`grep -rn "<<<<<<< \|>>>>>>> " --exclude-dir=.git .`).
3. The Flask app boots: start it briefly with the repo venv (e.g. `timeout 15 venv/bin/python webui.py` or the venv's flask entry — inspect the file header/start-beacon.sh for how it is launched) and confirm it serves `/` (HTTP 200) and accepts a POST to `/control` without 500. Kill it afterwards; do not leave processes running.
4. Sensors-related routes/handlers and the recording-path fix are both present in the resolved file.

## Constraints

- **Do NOT run `git add` or `git commit`.** Leave all changes uncommitted in the working tree; the orchestrator audits and commits.
- Only `webui.py` may be modified.
- Do not "improve" or refactor beyond the merge resolution.

## Report

In your final message list: what each conflict block contained on each side, how you merged them, and the output of the acceptance checks.
