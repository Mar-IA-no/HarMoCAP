# T2.1 — harmonic-shaper repo scaffolding

Agent: kimi. Size: S. Wave: 1.
Target: NEW directory ~/Projects/harmonic-shaper (does not exist yet — create it).
Context: harmonic-shaper will be the canonical additive synthesizer of the Harmonic Beacon ecosystem (32 voices + waveshaper + per-voice LFO + sidechain), extracted in a later task from ~/Projects/digital-beacon. This task is ONLY the empty scaffold with the ecosystem's repo conventions.

## Task

Create the repository skeleton:

```
harmonic-shaper/
├── pyproject.toml          # setuptools or hatchling; name "harmonic-shaper", version 0.1.0, requires-python >=3.10; MIT license metadata
├── LICENSE                 # MIT license text, copyright "AlterMundi contributors"
├── README.md               # purpose statement: canonical additive harmonic synth of the beacon ecosystem; status: scaffold, extraction from digital-beacon pending; will expose OSC namespace /shaper/*
├── BITACORA.md             # header + one line: repo scaffolded 2026-07-18
├── .gitignore              # venv/, .venv/, __pycache__/, *.pyc, *.wav, .env, *.egg-info/, dist/, build/
├── src/harmonic_shaper/__init__.py   # empty, or __version__ = "0.1.0"
├── configs/.gitkeep
├── docs/.gitkeep
└── tests/.gitkeep
```

Then: `git init`, `git add -A`, single commit `chore: repo scaffolding (conventions from HarMoCAP ecosystem)`.

## Acceptance criteria

1. `pip install -e . --no-deps` succeeds inside a throwaway venv (create it under /tmp, do not leave a venv inside the repo).
2. `git log --oneline` shows exactly one commit; `git status` clean.
3. No GitHub remote, no push — the orchestrator wires remotes separately.

## Constraints

- Do NOT copy any code from digital-beacon or NaturalHarmony — extraction is a later task.
- Do NOT create a venv inside the repo.
- English in all files.

## Report

Final message: tree of the created repo, the pip install check output, git log line.
