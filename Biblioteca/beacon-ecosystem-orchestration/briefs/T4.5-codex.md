# T4.5 — End-to-end integration rehearsal (NEVER descoped)

Agent: codex (`gpt-5.6-sol`, xhigh). Size: L. Final gate of the re-architecture.
Repos: harmonic-weaver (orchestrates), beacon-spatial, harmonic-shaper, HarMoCAP (fixture only), cymatic-control (simulators only).

## Pre-verified inventory (orchestrator, 2026-07-18 — use exactly these paths)

- `~/Projects/beacon-spatial/harmonic_beacon_2026_05_13_session.wav` — 659 MB file-mode source loop, present.
- `~/Projects/beacon-spatial/start-beacon.sh --file` — canonical launcher (scsynth 57110, sclang OSC 57120, Flask 5050).
- `~/Projects/harmonic-shaper` — standalone CLI `python -m harmonic_shaper` with `--no-audio/--no-midi/--no-osc/--api-*` flags; native MIDI source default-on; `/digital/*` OSC 9002; state API.
- `~/Projects/harmonic-weaver` — engine + Stage WS API (`python -m harmonic_weaver.weaver`), drivers: harmocap (fixture replay), midi (degrades invalid without hardware), ecg (OSC :5001 trigger/BPM).
- `~/Projects/HarMoCAP/examples/fixtures/two_persons.jsonl` — HarMoCAP replay fixture.
- `~/Projects/cymatic-control/test_ecg_stream.py`, `simulate_eeg.py` — existing simulators (ECG stream is the one wired to the weaver driver; EEG simulator is optional evidence only).
- `~/Projects/beacon-spatial/assets/nature-samples/dominicalito_frogs_pond.wav` — nature sample for `/beacon/nature/load`.

## Task

Build the full rehearsal harness **and run it**. Deliverable: `rehearsal/` in harmonic-weaver with:

1. `rehearsal/run_rehearsal.sh` (or Python orchestrator) that:
   - boots beacon-spatial in `--file` mode (no R24 needed);
   - boots harmonic-shaper headless (`--no-midi`, real audio device optional — if no device, use `--no-audio` and state-API evidence instead, declared explicitly);
   - boots harmonic-weaver with the three drivers;
   - replays `two_persons.jsonl` through the HarMoCAP driver and the ECG simulator stream through the ECG driver;
   - installs the real instrument manifests (beacon_spatial.contract.json + shaper.contract.json) and gates hello/sync on contract_id;
   - loads the demo scene (below) through the Stage WS API.

2. `rehearsal/scenes/event_demo.scene.json` — the event demo scene:
   - focus subject (HarMoCAP slot with focus) → one harmonic per limb + head: left/right wrist y, left/right ankle y, nose/head → 5 shaper voices (voice_gain + voice frequency or band gain), with scale_range transforms and min_confidence gating;
   - crowd aggregate (second person slot) → beacon master or band gains;
   - ECG trigger → rhythmic element (e.g. nature gain pulse or a band on/off);
   - nature layer loaded via `/beacon/nature/load` with the frogs sample at bounded gain.
   - a second scene (e.g. `sparse.scene.json`) for the hot-swap test.

3. Live-fire sequence (scripted, timed, logged):
   - run demo scene ≥ 90 s while replaying sources;
   - capture: weaver behavior reports (`reports/<run_id>/`), beacon `/beacon/state` dumps at t=0/mid/end, shaper state API snapshots at t=0/mid/end;
   - hot-swap to the second scene mid-run, capture state around the swap;
   - trigger global PANIC via Stage API, assert: shaper voices released (state API), beacon master→0/silence profile applied (state dump), routes gated;
   - clear panic, assert recovery (routes emit again);
   - clean shutdown of all four processes.

4. Audio evidence: have SuperCollider record its master output to a rehearsal WAV during the run (`/beacon/record/start|stop` exists in beacon.scd). Analyze the WAV offline: duration, non-silence ratio, peak, RMS, NaN check — prove signal flowed. Do NOT claim anyone heard it.

5. `rehearsal/REPORT.md` — timeline, every captured artifact path, numeric audio stats, state dump diffs, panic/recovery evidence, and explicit unverified items (e.g. audible monitoring by a human, R24 live input).

## Hard requirements

- No hardware: no R24, no MIDI devices, no people. MIDI driver must report invalid channels and that is expected evidence.
- Fail loudly: any process that dies or any assertion that fails aborts the rehearsal with non-zero exit and the partial evidence preserved.
- Everything runnable with one command from a clean checkout state (document required venvs).
- Do NOT commit. English only.

## Report

Full rehearsal transcript summary, artifact tree, audio stats, and exact pass/fail of each scripted assertion.