# T3.2 — Nature sample player in beacon-spatial with `/beacon/nature/*`

Agent: codex (`gpt-5.6-sol`, xhigh). Size: M.
Repo: ~/Projects/beacon-spatial (T1.2 formal OSC contract and T3.1 `nature/` package are merged).

## Authority (read first)

- `~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/04-digital-beacon.md` §2.A/B and `ROADMAP.md` T3.2.
- Existing target: `beacon.scd`, `beacon_spatial.contract.json`, `contract_codec.py`, `tests/test_osc_contract.py`, `start-beacon.sh`.
- Source reference only: `~/Projects/digital-beacon/beacon.scd`, specifically its `\\sample_player` SynthDef and sample OSC handlers. Preserve the target's 13-band live spatializer; do not import digital-beacon architecture.
- T3.1 modules: `nature/sample_layer.py` and `nature/resonant_filter.py`. This task adds playback, not descriptor modulation (T3.3).

## Task

1. Add a `\\sample_player` SynthDef to target `beacon.scd`, mixed through the existing spatializer output path without changing the 13-band DSP behavior. It must safely handle a loaded mono/stereo WAV, gain, and stop/release.

2. Add native OSC controls:
   - `/beacon/nature/load <path>`: load a local WAV and replace/free the preceding nature buffer safely;
   - `/beacon/nature/gain <float>`: bounded linear gain;
   - `/beacon/nature/stop`: fade/release playback and free the active synth safely.

3. Add those addresses to `beacon_spatial.contract.json` with exact types/ranges/readability and include their readable state in `/beacon/state` dumps. Regenerate its contract_id golden sidecar using the copied codec. Extend the existing manifest coverage tests.

4. Update `start-beacon.sh` only if strictly required. Do not add a new daemon or Python audio renderer.

5. Tests:
   - contract/golden/state-dump coverage for all three native addresses;
   - source extractor coverage including them;
   - if sclang/scsynth are available, boot the real target outside sandbox and run an OSC E2E: generate a tiny WAV in `/tmp`, load it, set gain, request state and verify nature fields, stop it. If audio cannot be objectively heard here, explicitly say audio is unconfirmed rather than claiming it sounded.

## Constraints

- Do NOT commit.
- Preserve existing `/beacon/*` semantics and 13-band spatializer.
- Input paths are local-only; reject or safely report invalid/unreadable files without crashing sclang.
- English only.

## Report

Synth routing summary, contract changes/count, state dump behavior, test output, and exact distinction between signal-path verification and audible confirmation.