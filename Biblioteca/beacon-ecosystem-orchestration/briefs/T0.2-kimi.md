# T0.2 — digital-beacon junk cleanup

Agent: kimi. Size: S. Wave: 1.
Repo: ~/Projects/digital-beacon (branch main; pre-existing local modifications: `M digital_beacon/sample_manager.py` and untracked PNGs under `data/uploads/` — DO NOT touch, revert, or stage them).
Spec: ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/04-digital-beacon.md §4 and §6.

## Task

Delete exactly this junk, nothing else:

1. **The directory literally named `: RTK && `** (repo root) — garbage from a mangled `mkdir -p`. Contains only empty nested dirs. Verify it contains no files before deleting (find with zero file count), then remove it. Quote/escape the name carefully.
2. **Broken symlinks under `normalized_analysis/`** — 37 symlinks whose targets were deleted. For every symlink in that directory, test `readlink -e`; delete ONLY the ones that are broken. If the directory is left empty, remove the directory itself.
3. **The duplicate virtualenv** — both `venv/` and `.venv/` exist. Test each one's Python with the project's key imports (`sounddevice`, `fastapi`, `librosa`, `numpy`). Keep the one that imports cleanly (if both work, keep `venv/`). Delete the other. Report which was kept and why.

## Hard exclusions — do NOT touch

- `data/uploads/` (nature samples — sacred), `originals/`, `data/sources/`, `packages/`, `tools/`
- `digital_beacon/sample_manager.py` (has legitimate local modifications)
- Any git-tracked file. If you believe a tracked file should be deleted, list it in your report instead of deleting it.

## Constraints

- **Do NOT run `git add` or `git commit`.**
- Before deleting, capture an inventory: `find` listing of each target. After deleting, confirm each path is gone.

## Report

Final message must include: inventory of what was deleted (paths + sizes), verification that hard exclusions are untouched (`git status --porcelain` before/after comparison), which venv was kept.
