# T3.2 repair — real OSC string load rejected

Agent: codex (`gpt-5.6-sol`, xhigh). Repo: ~/Projects/beacon-spatial.

T3.2 implementation exists in the working tree but its orchestrator E2E audit FAILED. Fix only this root cause, do not redesign the task.

## Exact reproduction evidence

1. Booted real `pw-jack scsynth -u 57110 -i 2 -o 2` and `QT_QPA_PLATFORM=offscreen sclang -u 57120 beacon.scd` successfully.
2. Generated a valid absolute mono WAV at `/tmp/beacon-nature-e2e.wav`.
3. Used the repo venv `python-osc` client to send:
   `Simple UDP OSC /beacon/nature/load`, one string arg `/tmp/beacon-nature-e2e.wav`.
4. sclang log says: `[NATURE] Rejected load without one OSC string path.`
5. `/beacon/nature/gain 0.35` works and `/beacon/state` returns nature gain 0.35, but `/beacon/nature/load` remains `""`.

The handler is reached; its message type guard rejects a valid standard OSC string. Current code at `beacon.scd` ~473 tests:
`(msg.size > 1) and: { msg[1].isKindOf(String) }`.

## Task

- Diagnose SuperCollider's actual received type for an OSC string from python-osc and change the guard/coercion to accept valid OSC string payloads while preserving strict rejection for missing/non-path values.
- Keep absolute-path, WAV-only, readable/non-empty, mono/stereo validation intact.
- Add a regression test that would have caught this language-side type compatibility issue (source-level only if unavoidable, but prefer a runnable local probe where possible).
- Re-run the exact real boot + generated-WAV load → gain → state → stop sequence. Require state dump to report the loaded path and gain before stop, then empty path after stop.

## Constraints

- Do NOT commit.
- Do not alter unrelated DSP/control behavior.
- Report exact before/after received type evidence and the full E2E output.
