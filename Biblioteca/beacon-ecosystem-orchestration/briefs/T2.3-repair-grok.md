# T2.3 repair — add co-band collision lifecycle regression

Agent: Grok. Repo: ~/Projects/harmonic-shaper.

T2.3 is otherwise implemented and manually audited. Add one focused regression test only; do not change product logic unless the test disproves it.

## Observed runtime behavior (headless, verified)

With f1=40.4 and anchor=24, MIDI notes 0 and 12 both map to store band 1.

```text
note_on(0)  -> voice_id 1000000
note_on(12) -> voice_id 1000001 replaces the active store-band owner
note_off(0) -> must NOT deactivate voice_id 1000001
note_off(12) -> releases the final active voice
```

The current `VoiceParameterStore` explicitly retains the most recent active voice per harmonic band. The native handler's required safety property is therefore: note-off for a superseded same-band note must never silence the current note.

## Task

- Add a direct test in `tests/test_native_midi.py` (or existing focused test file) selecting/using a deterministic same-band pair, asserting the lifecycle above.
- Run all T2.3 tests and the full suite.
- Do not commit. Report only files changed and test output.
