# T0.4 — beacon-spatial repo reorder (PD stack → legacy/, research → docs/)

Agent: kimi. Size: S. Wave: 2.
Repo: ~/Projects/beacon-spatial (branch main, clean tree at dispatch — webui.py merge already fixed and committed).
Spec: ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/03-beacon-spatial.md §1, §4, §6.

## Task

Reorganize the repo so the root contains ONLY the living stack. Use `git mv` for tracked files (preserves history). Target state:

1. **To `legacy/`** (the directory already exists — dedupe, don't duplicate):
   - `beacon-spatial.pd` — IDENTICAL to `legacy/beacon-spatial.pd`: delete the root copy instead of moving (verify byte-identity with `cmp` first).
   - `bridge.py` — IDENTICAL to `legacy/bridge.py`: same treatment (verify with `cmp`).
   - `spatializer~.pd` — move to `legacy/` (check whether legacy already has it; if identical, delete root copy).
   - `generate.py` — move to `legacy/`.
   - `beacon_pd_replica.scd` — move to `legacy/`.
   - `run-stack.sh` — move to `legacy/`.
   - Root `test_*` files (PD/OSC legacy tests) — move to `legacy/tests/` (create subdir); check what's there first.
   - `start-beacon-pd.sh` — move to `legacy/` (it launches the replica; note in your report that it references paths that may now be stale — do NOT fix the script, just flag it).
2. **`research/` → `docs/research/`** (git mv; create `docs/`).
3. **Delete** `beacon_record_260616_092811_/` (failed-recording junk dir, ~768 KB).
4. **Do NOT touch**: `beacon.scd`, `webui.py`, `start-beacon.sh`, `configs/`, `MEMORY.md`, `README.md`, `beacon-osc.json`, `beacon-osc.ANNOTATIONS.md`, `.grokignore`, `venv/`, the WAV file, `PD_REPLICA_OSC_SCHEME.md` (stays at root — it's the replica's doc; actually better: move it to `legacy/` next to the replica. Do that and say so).
5. After moving, check for references to moved paths in the files that stay (grep for `beacon_pd_replica`, `run-stack`, `bridge.py`, `research/` in root *.md, *.sh, *.py). Do NOT rewrite code references — list them in the report so the orchestrator decides.

## Acceptance criteria

- `git status` shows only renames (R) and deletions (D) consistent with the above; no content modifications.
- Root contains no `*.pd` files, no `run-stack.sh`, no `beacon_pd_replica.scd`, no `beacon_record_*`.
- `python3 -c "import ast; ast.parse(open('webui.py').read())"` still passes (sanity — you didn't touch it).

## Constraints

- **Do NOT run `git add` or `git commit`** — wait, exception: `git mv` stages renames by design and that's fine, but do NOT commit. Leave the index as-is for the orchestrator to review and commit.
- English in any note you add (you should not need to add files).

## Report

Tree before/after, dedupe decisions (what was identical), stale references found, sanity check output.
