# T6.1 — ARCHIVE.md for harmonic-beacon-tines

Agent: kimi. Size: S. Wave: 1.
Repo: ~/Projects/harmonic-beacon-tines (branch main; has pre-existing unrelated local modifications under `parts/` — do NOT touch them).
Spec: ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/05-harmonic-beacon-tines.md — READ IT FIRST; §2 (knowledge to preserve), §3 (reusable code), §4 (checklist) are the exact content sources.

## Task

Create a single new file `ARCHIVE.md` in the repo root that makes the frozen repo self-explanatory. Context: the physical tines hardware (ESP32-driven electromagnetic kalimba tines) no longer exists; the repo is archived as-is and git preserves everything.

The document must contain, in English:

1. **Declaration**: project archived (date 2026-07-18), hardware no longer exists, repo frozen for knowledge preservation. One paragraph on what the system was (see spec §1).
2. **High-priority preserved documents** — links with repo-relative paths and one line each on why they matter:
   - `NATURE_LAB_V2_DESIGN.md` (bioacoustic synthesis design treatise; "detail lives in per-sample modulation, not update rate")
   - `PERFORMANCE_ANALYSIS.md` (real-time architecture lessons: thread collapse, lock contention, OSC flooding −98%)
   - `MEMORY.md` (mental map + relationship to NaturalHarmony)
   - `.hermes/plans/2026-03-26_065740-my-friend-lets-plan-for-a-new-set.md` (expression-plugin mathematics: BREATHE, RESONANCE sympathetic coupling, etc.)
3. **Reusable code map** — path + one line each, from spec §3: `synth_beacon/` (hardware-independent additive synth engine), Nature Lab logic (`beacon_daemon/nature_renderer.py`), real-time patterns (`beacon_core/state.py`, `beacon_daemon/osc_sender.py` — batch_update, lock-free snapshots, OSC coalescing), OSC schema (`beacon_core/config.py`), MIDI mappings (`beacon_daemon/control/`), `beacon_core/session_manager.py` (presets), `parts/sun/` (parametric CAD, build123d).
4. **Note**: the Nature Lab bioacoustic generators are candidates for a future generative nature layer in the beacon ecosystem — annotated, not migrated.
5. **Hardware reference** (only if hardware is ever rebuilt): `esp32_tines_beacon/README.md`, `parts/sun/`.

Verify every linked path actually exists in the repo before writing it (`test -e`). Fix any path that differs from the spec and say so in the report.

## Constraints

- Only create `ARCHIVE.md`. Modify nothing else. **Do NOT run `git add` or `git commit`.**

## Report

Final message: the full ARCHIVE.md content, plus the list of spec paths that needed correction (if any).
