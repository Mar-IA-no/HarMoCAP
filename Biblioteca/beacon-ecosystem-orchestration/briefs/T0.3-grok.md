# T0.3 — beacon-spatial documentation truth-alignment

Agent: grok. Size: S. Wave: 1.
Repo: ~/Projects/beacon-spatial (branch main).
Spec: ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/03-beacon-spatial.md — READ IT FIRST, it is the authority for this task (especially §1, §2, §5). The code is the source of truth; docs currently contradict it.

## Task

Fix three documentation artifacts so they match the real, current system. All facts below are already verified in the spec — do not re-survey the codebase beyond confirming what you edit.

1. **`README.md`** — currently describes an obsolete 6-band Pure Data system with `extracto_2min.wav`. Rewrite it to describe the CURRENT engine: `beacon.scd` SuperCollider 13-band binaural spatializer (12 BPF + 1 HPF @1800 Hz, ATK FoaPanB/FoaDecode, Listen HRTF kernels @48 kHz), OSC control on port 57120, `start-beacon.sh` launcher (`pw-jack scsynth` on 57110, `--live` default from Zoom R24 CH1, `--file` WAV loop mode using `harmonic_beacon_2026_05_13_session.wav`), `webui.py` Flask UI on :5050, PD stack as legacy (being moved to `legacy/`). Keep it concise; document the real files, not aspirations.
2. **OSC table in `MEMORY.md`** — currently documents an old scheme with `bandIdx` as a message argument. Replace it with the real scheme (69 OSCdefs, one float per message, band index EMBEDDED in the address): `/beacon/gain/N` (0–3, N=1..13), `/beacon/az/N` (-180..180), `/beacon/dist/N` (0..10), `/beacon/q/N` (N=1..12), `/beacon/solo/N` (0/1), `/beacon/mix` (0..1), `/beacon/master` (0..3), `/beacon/record/start`, `/beacon/record/stop`, `/beacon/reset`. Note the replica engine on port 9001 uses the same scheme for N=1..6 only.
3. **`beacon-osc.json`** (Open Stage Control template) — annotate the addresses that have NO destination in the current engine: `/beacon/wet`, `/beacon/dry` (the engine uses `/beacon/mix`), and `/beacon/lfo/offset`. JSON has no comments: either add `"_unmapped"` fields in-place or create a short companion `beacon-osc.ANNOTATIONS.md`. Your choice; explain it in the report.

## Constraints

- **Do NOT run `git add` or `git commit`.** Leave changes uncommitted.
- Only these three files (plus optionally the new ANNOTATIONS file) may be modified/created.
- Do not touch `webui.py` or any code file — another agent is working there concurrently.
- English for all new/rewritten content.

## Report

List per file: what was wrong, what you wrote, and any place where the spec and the code seemed to disagree (do not resolve those — flag them).
