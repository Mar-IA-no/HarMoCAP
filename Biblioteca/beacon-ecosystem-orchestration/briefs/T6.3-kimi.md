# T6.3 — ARCHIVE.md for digital-beacon and NaturalHarmony

Agent: kimi. Size: M.
Repos: ~/Projects/digital-beacon and ~/Projects/NaturalHarmony.

## Context

The beacon ecosystem re-architecture is complete for the migration map in `~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/04-digital-beacon.md` §6. Both legacy repos now get their final archive documents, modeled on `~/Projects/harmonic-beacon-tines/ARCHIVE.md` (read it as the template).

## Destination map (verified by orchestrator — use exactly this in the docs)

**digital-beacon:**
- Shaper (audio_engine, state, config shaper sections, midi_control, osc_receiver /digital branch, /api/shaper endpoints, synth_pure) → `harmonic-shaper` (standalone, `pip install -e .`, contracts/shaper.contract.json).
- resonant_filter + sample_layer (+ vendorized nh mask) → `beacon-spatial/nature/`.
- sample_player + `\sample_player` SynthDef → `beacon-spatial` `beacon.scd` + `/beacon/nature/*`.
- beacon-half of sample_modulator (4 presets) → `beacon-spatial/nature/sample_modulator.py`; shaper-half deferred to F5 (explicitly unimplemented, by design).
- nature samples → `beacon-spatial/assets/nature-samples/` (gitignored + SHA-256 MANIFEST).
- routing/modulation future home → `harmonic-weaver`.
- archived/discarded by earlier tasks: packages/ nh-toolkit v2 (apartado), normalized_analysis/ + originals/ (broken one-off symlinks, removed T0.2), : RTK && (junk, removed T0.2), duplicate venvs (removed T0.2).

**NaturalHarmony:**
- harmonic mapping (harmonics.py, key_mapper.py) → ported dependency-free into `harmonic-shaper/src/harmonic_shaper/harmonic_mapping.py` (T2.3).
- Shaper lineage → evolved into digital-beacon fork, now canonical at `harmonic-shaper`.
- Launchpad/visualizer concepts → superseded by harmonic-weaver routing + future F5 work.

## digital-beacon working-tree state (orchestrator-verified, handle exactly)

- `M digital_beacon/sample_manager.py`: pre-existing one-line change (master gain 0.0 → 0.8 with comment). Commit it as a final-state commit with message noting it is a legacy default fix preserved for the archive.
- 4 untracked PNGs in `data/uploads/` (regenerable spectrograms/waveforms): do NOT commit; leave untracked.
- `data/uploads/test.wav`, `test_tone.wav`, `test-upload.json`: leave as-is (tiny, harmless).

## Task

1. digital-beacon: commit the sample_manager.py change (final-state legacy fix). Then write `ARCHIVE.md` (English): purpose-retired statement, full destination map above, what is archived/discarded, pointer to harmonic-shaper/beacon-spatial/harmonic-weaver as successors, and a note that the repo remains readable history.
2. NaturalHarmony: write `ARCHIVE.md` with its destination map above.
3. Do NOT push. Do NOT delete anything. Do NOT touch other files.

## Report

ARCHIVE.md paths, the final-state commit hash for digital-beacon, and `git status --short` of both repos after work.