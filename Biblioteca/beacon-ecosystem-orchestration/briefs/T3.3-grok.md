# T3.3 — Beacon-only descriptor modulation split

Agent: Grok. Size: M.
Repo: ~/Projects/beacon-spatial. T3.1 (`nature/` analysis) and T3.2 (native `/beacon/nature/*` player) are merged.

## Authority (read first; do not re-explore)

- Source extraction boundary: `~/Projects/digital-beacon/digital_beacon/sample_modulator.py` and `sample_manager.py`.
- Target descriptor source: `nature/sample_layer.py`, especially `SampleDescriptor.to_dict()`.
- Target OSC surface: `beacon_spatial.contract.json` and `beacon.scd`.
- `requirements.txt` already declares `python-osc`; use an injectable sender/transport seam so tests require no UDP listener.

## Task

Port only the `target_type == "beacon"` half of the legacy modulation layer into `nature/` (for example `nature/sample_modulator.py` and an optional small manager). Do not import `harmonic-shaper`, `VoiceParameterStore`, `digital_beacon`, or any Shaper path.

1. Preserve declarative target mapping with descriptor normalization, range clamp, threshold, invert, and EWMA smoothing.
2. Permit only controls formalized by current beacon-spatial contract:
   - global: `/beacon/master`, `/beacon/f1`, `/beacon/vsource`;
   - band: `/beacon/gain/{band}`, `/beacon/az/{band}`, `/beacon/dist/{band}`, `/beacon/q/{band}`, `/beacon/on/{band}`.
   Validate band bounds against the actual manifest (13); reject any legacy 32-band invalid target, never emit it.
3. Carry only the four designated presets, stripped to Beacon targets:
   - `spectrum-projection`
   - `harmonic-projection`
   - `consonance-gate`
   - `timbre-filter`
   Document their mapping semantics as design choices, not effects/clinical claims.
4. Integrate at a minimal Python seam: a sample descriptor can be fed to the modulator; optional manager may connect `SampleLayer.on_descriptor` and invoke T3.2 load/gain/stop routes only if this is cleanly testable. No Flask/UI work and no new long-lived daemon.
5. Add a transport recording fake and focused tests for descriptor→bounded OSC addresses/values, clamp/threshold/smoothing, invalid target rejection, and each preset. At least one test must confirm no Shaper import exists in target runtime source.

## Constraints

- No commits.
- No `/digital/*`, Shaper state, or Shaper preset output; those belong to deferred F5 work.
- Do not modify existing SuperCollider routing or contract addresses.
- English only.

## Report

Module map, explicit excluded legacy targets, preset table, full test output, and a recorder-based descriptor-to-OSC demonstration.