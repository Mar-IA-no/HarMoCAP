# T1.3 — Shaper OSC manifest (formalize /digital/* over existing code)

Agent: grok. Size: S. Wave: 3.
Repos involved: read spec + code in ~/Projects/digital-beacon; deliver in ~/Projects/harmonic-shaper.
Specs (READ FIRST):
- ~/Projects/HarMoCAP/Biblioteca/rearquitectura-ecosistema-beacon/reportes_agentes/04-digital-beacon.md §1, §3, §5 (the Shaper: audio_engine.py, state.py, osc_receiver.py, config.py; 32 voices, waveshaper, per-voice LFO, sidechain; OSC :9002 `/digital/*` and :9001 NH broadcasts).
- ~/Projects/harmonic-weaver/contracts/instrument_contract.template.json — the manifest mold. Follow it exactly.
- ~/Projects/harmonic-weaver/src/harmonic_weaver/contract_codec.py — copy into harmonic-shaper (codec travels by copy) and use it for the contract_id.

## Task

Produce `contracts/shaper.contract.json` (+ golden sidecar) in ~/Projects/harmonic-shaper, formalizing the OSC surface the Shaper ALREADY exposes in digital-beacon code (this manifest will travel with the code extraction in a later task — you are documenting existing behavior, not designing new one):

1. Read `digital_beacon/osc_receiver.py`, `config.py`, `state.py`, `midi_control.py`, and the `/api/shaper/*` endpoints in `api.py` to extract the real address table: every OSC address the shaper listens to, with arg types and ranges. Note BOTH listeners: `:9002` (`/digital/*` direct control) and `:9001` (NaturalHarmony broadcast slave mode, e.g. `/beacon/voice/*` — check exact addresses in the code).
2. Fill the Instrument Control v1 template: namespace, capabilities (per-address types/ranges), handshake (or explicit "none today — to be added" section if the code has no handshake), state_sync section (the WebSocket `/ws` state broadcast in api.py IS a form of state sync — document it as the current mechanism + note the planned OSC `/shaper/state` dump as `planned`), receiver rules.
3. **Include the optional `voice_model_alias` section** — the shaper DOES fit the BCP common-voice model (per-voice gain/pan/phase over harmonic_n 1..32 + f1 + on/off). Map the native addresses to the alias explicitly.
4. Copy `contract_codec.py` from harmonic-weaver into `src/harmonic_shaper/contract_codec.py` (adjust nothing — it must stay byte-identical for the copy-pattern to hold; if it needs changes, flag instead). Generate contract_id + golden sidecar.
5. `tests/test_shaper_contract.py`: manifest validates, contract_id matches sidecar, and every OSC address found in `digital_beacon/osc_receiver.py` (write a small extractor over the source file) is covered by the manifest.

## Constraints

- **Do NOT commit** in either repo. Leave changes in working trees.
- Do not modify anything in digital-beacon — read-only there.
- If the code contradicts the report, the CODE wins — flag the discrepancy in your report.
- English in all artifacts.

## Report

Address table (count by listener), discrepancies found between report and code, alias mapping summary, test output.
